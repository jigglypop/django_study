let Koa,Router,bodyParser,mongoose;_5ec‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_5ec‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_5ec‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_5ec‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);require("dotenv").config();





const { PORT, MONGO_URI } = process.env;
mongoose
  .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
  .then(() => {
    console.log("connectDB");
  })
  .catch(e => {
    _5ec‍.g.console.error;
  });

const api = require("./api");

const app = new Koa();
const router = new Router();

app.use(bodyParser());

router.use("/api", api.routes());

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  console.log("start");
});
